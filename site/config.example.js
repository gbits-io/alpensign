// AlpenSign — Local Configuration Template
// Copy this file to config.js and fill in your keys.
// config.js is in .gitignore — never commit real keys.

const ALPENSIGN_CONFIG = {
  // Helius RPC — free tier: https://dev.helius.xyz
  // Required: public Solana RPCs block browser requests (403).
  HELIUS_API_KEY: 'YOUR_HELIUS_API_KEY',
};
